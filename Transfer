<ng-template let-uc pTemplate="gridItem">
  <div class="col-12 md:col-6 lg:col-4 xl:col-3 p-2">
    <div class="agent-card" (click)="navigate(uc.usecase_id, uc.usecase_name)">
      <!-- Circular Icon -->
      <div class="agent-icon">
        <span class="material-symbols-outlined notranslate" style="font-size:36px;">
          {{ uc.thumbnail ? '' : 'smart_toy' }}
        </span>
        <img *ngIf="uc.thumbnail" class="thumbnail" [src]="uc.thumbnail" />
      </div>

      <!-- Agent Title -->
      <h3 class="agent-name">{{ uc.usecase_name }}</h3>

      <!-- Category / Model -->
      <p class="agent-category">{{ uc.model_name || 'General' }}</p>

      <!-- View Button -->
      <button class="view-btn" (click)="navigate(uc.usecase_id, uc.usecase_name); $event.stopPropagation()">
        View
      </button>
    </div>
  </div>
</ng-template>
