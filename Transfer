import { CopilotRuntime, HttpAgent } from "@copilotkit/runtime";

export const runtime = new CopilotRuntime({
  agents: {
    custom: new HttpAgent({
      url: "http://localhost:8000/copilotkit_remote", // Backend endpoint
    }),
  },
});

// src/App.jsx
import { CopilotChat } from "@copilotkit/react-ui";

function App() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>AG UI Chat - React + Custom LLM</h1>
      <CopilotChat
        labels={{
          title: "Custom AI Agent",
          initial: "Ask me anything...",
        }}
      />
    </div>
  );
}

export default App;


// src/main.jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import { CopilotKit } from "@copilotkit/react-core";
import { runtime } from "./copilotRuntime";

import "@copilotkit/react-ui/styles.css"; // Required

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CopilotKit runtime={runtime} agent="custom">
      <App />
    </CopilotKit>
  </React.StrictMode>
);
